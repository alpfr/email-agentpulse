# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Secrets (never bake into image)
.env
credentials.json
token.json

# Frontend (has its own Dockerfile)
dashboard/

# IDE / OS
.git/
.DS_Store
*.swp
*.swo

# Tests / misc
tests/
*.zip
*.tar.gz

# EKS / K8s (deployment only)
eks/
k8s/
