apiVersion: apps/v1
kind: Deployment
metadata:
  name: email-agentpulse-backend
  namespace: email-agent
  labels:
    app: email-agentpulse
    component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: email-agentpulse
      component: backend
  template:
    metadata:
      labels:
        app: email-agentpulse
        component: backend
    spec:
      containers:
      - name: backend
        image: 713220200108.dkr.ecr.us-east-1.amazonaws.com/email-agentpulse-backend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
          name: http
          protocol: TCP
        envFrom:
        - configMapRef:
            name: email-agentpulse-config
        - secretRef:
            name: email-agentpulse-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: email-agentpulse-backend
  namespace: email-agent
  labels:
    app: email-agentpulse
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: email-agentpulse
    component: backend
