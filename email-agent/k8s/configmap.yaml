apiVersion: v1
kind: ConfigMap
metadata:
  name: email-agent-config
  namespace: email-agent
data:
  # Application
  APP_NAME: "Email AgentPulse"
  APP_ENV: "production"
  DEBUG: "false"
  LOG_LEVEL: "INFO"

  # API Configuration
  API_HOST: "0.0.0.0"
  API_PORT: "8000"

  # AWS Configuration
  AWS_REGION: "us-east-1"
  AWS_ACCOUNT_ID: "713220200108"

  # LLM Configuration
  LLM_PROVIDER: "anthropic"
  LLM_MODEL: "claude-sonnet-4-20250514"

  # Cognito
  COGNITO_REGION: "us-east-1"
